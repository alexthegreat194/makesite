<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Makesite</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

<div class="min-h-screen bg-gradient-to-r from-indigo-400 to-cyan-400 p-10">
  <div class="flex flex-col gap-5 rounded-xl bg-white bg-opacity-60 p-10 text-center">
    <h1 class="text-5xl font-bold">{{ .Heading }}</h1>
    {{ range .Body }}
    <p class="text-xl">{{ . }}</p>
    {{ end }}
  </div>
</div>

</body>
</html>
